
    // Render principal de la tabla de clientes
    window.renderCostumerTable = function(leadsArray) {
      console.log('[renderCostumerTable] Iniciando render con', (leadsArray||[]).length, 'leads');
